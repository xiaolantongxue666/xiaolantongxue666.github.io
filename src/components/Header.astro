---
import { SITE } from '../config';

const nav = [
  { label: '项目', href: '/projects' },
  { label: '博客', href: '/blog' },
  { label: '经历', href: '/#experience' },
  { label: '关于', href: '/about' },
];

const { path = Astro.url.pathname } = Astro.props as { path?: string };
const isActive = (href: string) => {
  // 锚点链接（页内跳转）不参与导航激活态
  if (href.includes('#')) return false;
  return path === href || path.startsWith(`${href}/`);
};
---

<header class="sticky top-0 z-40 border-b border-line bg-paper">
  <div class="mx-auto flex h-16 max-w-[1200px] items-center justify-between px-5 md:px-8">
    <a
      href="/"
      class="text-[15px] font-semibold tracking-tight text-ink transition-colors hover:text-accent"
    >
      {SITE.name}
    </a>

    <!-- 桌面导航 -->
    <nav aria-label="主导航" class="hidden items-center gap-6 md:flex">
      {
        nav.map((item) => (
          <a
            href={item.href}
            aria-current={isActive(item.href) ? 'page' : undefined}
            class:list={[
              'text-sm transition-colors',
              isActive(item.href)
                ? 'font-medium text-accent underline decoration-accent/40 underline-offset-8'
                : 'text-muted hover:text-ink',
            ]}
          >
            {item.label}
          </a>
        ))
      }
      <a
        href={`mailto:${SITE.email}`}
        class="ml-1 rounded-lg border border-line px-3.5 py-1.5 text-sm text-ink transition-colors hover:border-accent hover:text-accent"
      >
        联系我
      </a>
    </nav>

    <!-- 移动端菜单按钮 -->
    <button
      id="menu-btn"
      type="button"
      class="flex h-10 w-10 items-center justify-center rounded-md text-ink md:hidden"
      aria-expanded="false"
      aria-controls="mobile-menu"
      aria-label="打开菜单"
    >
      <svg id="icon-open" width="20" height="20" viewBox="0 0 20 20" fill="none" aria-hidden="true">
        <path d="M3 5.5h14M3 10h14M3 14.5h14" stroke="currentColor" stroke-width="1.6" stroke-linecap="round"></path>
      </svg>
      <svg id="icon-close" width="20" height="20" viewBox="0 0 20 20" fill="none" class="hidden" aria-hidden="true">
        <path d="M5 5l10 10M15 5L5 15" stroke="currentColor" stroke-width="1.6" stroke-linecap="round"></path>
      </svg>
    </button>
  </div>

  <!-- 移动端菜单面板 -->
  <nav
    id="mobile-menu"
    aria-label="移动端导航"
    class="hidden border-t border-line bg-paper md:hidden"
  >
    <div class="mx-auto flex max-w-[1200px] flex-col px-5 py-3">
      {
        nav.map((item) => (
          <a
            href={item.href}
            class="border-b border-line py-3 text-[15px] text-ink last:border-0"
          >
            {item.label}
          </a>
        ))
      }
      <a href={`mailto:${SITE.email}`} class="py-3 text-[15px] font-medium text-accent">
        联系我
      </a>
    </div>
  </nav>
</header>

<script>
  const btn = document.getElementById('menu-btn');
  const menu = document.getElementById('mobile-menu');
  const iconOpen = document.getElementById('icon-open');
  const iconClose = document.getElementById('icon-close');

  function setOpen(open: boolean) {
    if (!btn || !menu || !iconOpen || !iconClose) return;
    menu.classList.toggle('hidden', !open);
    iconOpen.classList.toggle('hidden', open);
    iconClose.classList.toggle('hidden', !open);
    btn.setAttribute('aria-expanded', String(open));
    btn.setAttribute('aria-label', open ? '关闭菜单' : '打开菜单');
  }

  btn?.addEventListener('click', () => {
    setOpen(menu?.classList.contains('hidden') ?? false);
  });

  // 点击菜单项后收起
  menu?.querySelectorAll('a').forEach((a) => a.addEventListener('click', () => setOpen(false)));
</script>
